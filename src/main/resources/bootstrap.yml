# Tomcat
server:
  port: 9902
# Spring
spring:
  application:
    # 应用名称
    name: foo
  cloud:
    nacos:
      discovery:
        # 服务注册地址
        server-addr: kx-prev:8848
        namespace: foo
      config:
        # 配置中心地址
        server-addr: kx-prev:8848
        namespace: foo
